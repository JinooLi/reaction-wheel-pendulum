#include "inc/interface.h"

int main() {
    interface.print("Hello, World!");  // Call the print function through the interface
    interface.test1(42, 84);           // Call the test1 function through the interface
    return 0;
}